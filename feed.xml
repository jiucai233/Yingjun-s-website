<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://jiucai233.github.io/Yingjun-s-website/feed.xml" rel="self" type="application/atom+xml"/><link href="https://jiucai233.github.io/Yingjun-s-website/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-01T15:50:25+00:00</updated><id>https://jiucai233.github.io/Yingjun-s-website/feed.xml</id><title type="html">blank</title><subtitle>AI Engineer focusing on computer vision, generative models, and multi-language AI systems.</subtitle><entry><title type="html">Paper Review: 3D-VLA — Bridging 3D Representations into Vision-Language-Action Models</title><link href="https://jiucai233.github.io/Yingjun-s-website/blog/2026/3d-vla-paper-review/" rel="alternate" type="text/html" title="Paper Review: 3D-VLA — Bridging 3D Representations into Vision-Language-Action Models"/><published>2026-03-15T00:00:00+00:00</published><updated>2026-03-15T00:00:00+00:00</updated><id>https://jiucai233.github.io/Yingjun-s-website/blog/2026/3d-vla-paper-review</id><content type="html" xml:base="https://jiucai233.github.io/Yingjun-s-website/blog/2026/3d-vla-paper-review/"><![CDATA[<blockquote> <p>Why train separate decoders and re-align them, when you could decode multiple representations in a single high-dimensional space?</p> </blockquote> <h2 id="1-3d-dataset-construction">1. 3D Dataset Construction</h2> <p>Since existing datasets lack the annotation density needed for 3D-aware VLA training, the authors built a custom data pipeline to curate 2D video/image data into rich 3D annotations:</p> <blockquote> <p>Obtaining annotations for point clouds, depth maps, 3D bounding boxes, the robot’s 7D actions, and textual descriptions.</p> </blockquote> <p><strong>Depth</strong> — Used ZoeDepth to extract depth from raw images, then multiplied by a normalization coefficient.</p> <p><strong>Optical Flow</strong> — Applied RAFT to generate flow maps, enabling the model to distinguish moving objects from static background.</p> <p><strong>3D Bounding Boxes</strong> — Used spaCy for object name extraction, then SAM for mask generation, which are converted to 3D bounding boxes.</p> <h2 id="2-interaction-tokens">2. Interaction Tokens</h2> <p>When generating image captions, the authors introduced special <strong>interaction tokens</strong> to help the model converge faster and better distinguish objects:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&lt;</span><span class="n">obj</span><span class="o">&gt;</span> <span class="n">a</span> <span class="n">chocolate</span> <span class="n">bar</span> <span class="o">&lt;/</span><span class="n">obj</span><span class="o">&gt;</span> <span class="p">[</span><span class="n">loc</span> <span class="n">tokens</span><span class="p">]</span> <span class="n">on</span> <span class="n">the</span> <span class="n">table</span>
</code></pre></div></div> <table> <thead> <tr> <th>Token</th> <th>Purpose</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">&lt;obj&gt;</code> <code class="language-plaintext highlighter-rouge">&lt;/obj&gt;</code></td> <td>Enclose the object name</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">&lt;loc0-255&gt;</code></td> <td>Ground referred objects in space</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">&lt;scene&gt;</code> <code class="language-plaintext highlighter-rouge">&lt;/scene&gt;</code></td> <td>Enclose embeddings of a static scene</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">&lt;aloc0-255&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;arot0-255&gt;</code></td> <td>Robot location and rotation control</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">&lt;gripper0/1&gt;</code></td> <td>Gripper open/close</td> </tr> </tbody> </table> <p>All tokens here are <strong>discrete</strong>, which means in practice this model may not be directly applicable to industrial scenarios requiring continuous control precision.</p> <h2 id="3-diffusion-models-and-training-objectives">3. Diffusion Models and Training Objectives</h2> <p>The model can “imagine” goal scenarios using two pre-trained diffusion models, which are later re-aligned via a transformer-based projector:</p> <blockquote> <p>For RGBD-to-RGBD generation, we employ Stable Diffusion V1.4 […] We concatenate the RGB latent and depth latent as the image condition. For point-to-point generation, we use Point-E, to which we add a point cloud condition input.</p> </blockquote> <p>For alignment, the authors used <strong>LoRA</strong> to fine-tune the diffusion models and an LLM to avoid catastrophic forgetting. The paper doesn’t elaborate on the loss design — because it’s quite straightforward. From the code:</p> <h3 id="t5-llm-objective--cross-entropy">T5 LLM Objective — Cross Entropy</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">outputs</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">t5_model</span><span class="p">(</span>
    <span class="n">inputs_embeds</span><span class="o">=</span><span class="n">inputs_embeds</span><span class="p">,</span>
    <span class="n">attention_mask</span><span class="o">=</span><span class="n">encoder_atts</span><span class="p">,</span>
    <span class="n">decoder_attention_mask</span><span class="o">=</span><span class="n">output_tokens</span><span class="p">.</span><span class="n">attention_mask</span><span class="p">,</span>
    <span class="n">return_dict</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
    <span class="n">labels</span><span class="o">=</span><span class="n">targets</span><span class="p">,</span>
<span class="p">)</span>
<span class="n">loss</span> <span class="o">=</span> <span class="n">outputs</span><span class="p">.</span><span class="n">loss</span>
</code></pre></div></div> <h3 id="goal-image-diffusion--mse">Goal Image Diffusion — MSE</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">model_pred</span> <span class="o">=</span> <span class="nf">unet</span><span class="p">(</span><span class="n">concatenated_noisy_latents</span><span class="p">,</span> <span class="n">timesteps</span><span class="p">,</span> 
                  <span class="n">encoder_hidden_states</span><span class="p">).</span><span class="n">sample</span>
<span class="n">loss</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">mse_loss</span><span class="p">(</span><span class="n">model_pred</span><span class="p">.</span><span class="nf">float</span><span class="p">(),</span> <span class="n">target</span><span class="p">.</span><span class="nf">float</span><span class="p">(),</span> <span class="n">reduction</span><span class="o">=</span><span class="sh">"</span><span class="s">mean</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <h3 id="goal-point-cloud-diffusion--mse">Goal Point Cloud Diffusion — MSE</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">noise_pred</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">noisy_pointcloud</span><span class="p">,</span> <span class="n">timesteps</span><span class="p">,</span> <span class="n">pointcloud</span><span class="p">,</span> <span class="n">texts</span><span class="o">=</span><span class="n">text</span><span class="p">)</span>
<span class="n">loss</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">mse_loss</span><span class="p">(</span><span class="n">noise_pred</span><span class="p">,</span> <span class="n">noise</span><span class="p">)</span>
</code></pre></div></div> <p>The loss design is intuitive: cross-entropy for the LLM, MSE for diffusion denoising.</p> <h2 id="4-transformer-roles-and-feature-alignment">4. Transformer Roles and Feature Alignment</h2> <p>The transformer architecture appears <strong>three times</strong> in the pipeline, each serving a different alignment role:</p> <ol> <li><strong>Q-Former</strong> — Compresses and translates 3D features into the text feature space. Accepts current images and “imagined” multimodal data, outputs the final action.</li> <li><strong>Pretrained LLM (T5)</strong> — Accepts translated 3D input + text input, generates text with interaction tokens.</li> <li><strong>Transformer-based Projector</strong> — Projects LLM output into the diffusion model’s latent space for goal imagination.</li> </ol> <p>The pattern: Q-Former handles <strong>image → text</strong>, the projector handles <strong>text → image embedding</strong>. These translators ensure the system works across modalities.</p> <h2 id="5-my-opinion">5. My Opinion</h2> <p><strong>Speed concerns.</strong> The paper doesn’t mention action frequency, but with 2 latent diffusion models and 3 transformers for alignment/generation, the inference latency is likely prohibitive for real-time industrial deployment.</p> <p><strong>Value of the work.</strong> As a study on integrating 3D features into VLA, this is an excellent reference. The dataset pipeline alone is a valuable contribution.</p> <p><strong>Future direction.</strong> The entire system is built on pre-trained modules stitched together. With recent breakthroughs in diffusion transformers (DiT), combining these separate modules into a <strong>single model with a unified objective</strong> seems like the natural next step — and would address the latency problem.</p> <p><strong>The fundamental question:</strong> Does 3D-VLA need to be better at <em>accuracy</em> or <em>response frequency</em>? Is it too redundant for a VLA model to process full 3D representations when a well-designed 2D system (like OpenVLA) already achieves decent generalization?</p>]]></content><author><name></name></author><category term="paper-review"/><category term="VLA"/><category term="robotics"/><category term="3D"/><category term="English"/><summary type="html"><![CDATA[Reviewing 3D-VLA's approach to injecting 3D understanding into VLA models — custom dataset construction, interaction tokens, dual diffusion model goal imagination, and multi-stage transformer alignment.]]></summary></entry><entry><title type="html">Paper Review: OpenVLA — Open-Source Vision-Language-Action Model</title><link href="https://jiucai233.github.io/Yingjun-s-website/blog/2026/openvla-paper-review/" rel="alternate" type="text/html" title="Paper Review: OpenVLA — Open-Source Vision-Language-Action Model"/><published>2026-03-12T00:00:00+00:00</published><updated>2026-03-12T00:00:00+00:00</updated><id>https://jiucai233.github.io/Yingjun-s-website/blog/2026/openvla-paper-review</id><content type="html" xml:base="https://jiucai233.github.io/Yingjun-s-website/blog/2026/openvla-paper-review/"><![CDATA[<h2 id="key-questions">Key Questions</h2> <p><strong>Q: What is the backbone model?</strong></p> <p>The backbone is <strong>Llama 2 (7B)</strong>. The VLM (Prismatic) uses this as its language model, combined with a dual vision encoder.</p> <p><strong>Q: What’s the most important innovation?</strong></p> <p>OpenVLA proved that even a <strong>7B model</strong>, with the right data mixture and vision backbone design, can achieve decent generalization in robotic manipulation while consuming far less compute than larger alternatives. And it’s fully open-sourced.</p> <hr/> <h2 id="core-architecture">Core Architecture</h2> <h3 id="1-dual-vision-backbone">1. Dual Vision Backbone</h3> <p>The vision encoder consists of two complementary modules:</p> <ul> <li><strong>DinoV2</strong> — Provides precise object shape and depth information. Without it, the robot knows <em>what</em> the object is but cannot grasp it precisely.</li> <li><strong>SigLIP</strong> — Combines object recognition with semantic/language alignment. Without it, the robot knows the object’s shape and depth but cannot relate it to a language prompt.</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Two modules' features are simply concatenated
</span><span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">pixel_values</span><span class="p">):</span>
    <span class="n">dino_patches</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">dino_featurizer</span><span class="p">(</span><span class="n">pixel_values</span><span class="p">[</span><span class="sh">"</span><span class="s">dino</span><span class="sh">"</span><span class="p">])</span>
    <span class="n">siglip_patches</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">siglip_featurizer</span><span class="p">(</span><span class="n">pixel_values</span><span class="p">[</span><span class="sh">"</span><span class="s">siglip</span><span class="sh">"</span><span class="p">])</span>
    <span class="k">return</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">dino_patches</span><span class="p">,</span> <span class="n">siglip_patches</span><span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">2</span><span class="p">)</span>
</code></pre></div></div> <p>An important detail: the authors extract features from the <strong>second-to-last layer</strong> (<code class="language-plaintext highlighter-rouge">n={len(blocks) - 2}</code>), since the final layer’s features lean toward classification while the penultimate layer retains more spatial/action-relevant information.</p> <h3 id="2-action-tokenization">2. Action Tokenization</h3> <p>The key insight is converting continuous robot actions into discrete tokens that the LLM can generate:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">ActionTokenizer</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">tokenizer</span><span class="p">,</span> <span class="n">bins</span><span class="o">=</span><span class="mi">256</span><span class="p">,</span> <span class="n">min_action</span><span class="o">=-</span><span class="mi">1</span><span class="p">,</span> <span class="n">max_action</span><span class="o">=</span><span class="mi">1</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">bins</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">linspace</span><span class="p">(</span><span class="n">min_action</span><span class="p">,</span> <span class="n">max_action</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">n_bins</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">bin_centers</span> <span class="o">=</span> <span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">bins</span><span class="p">[:</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="n">bins</span><span class="p">[</span><span class="mi">1</span><span class="p">:])</span> <span class="o">/</span> <span class="mf">2.0</span>
        <span class="n">self</span><span class="p">.</span><span class="n">action_token_begin_idx</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="n">tokenizer</span><span class="p">.</span><span class="n">vocab_size</span> <span class="o">-</span> <span class="p">(</span><span class="n">bins</span> <span class="o">+</span> <span class="mi">1</span><span class="p">))</span>
</code></pre></div></div> <p>Three steps:</p> <ol> <li><strong>Initialize bins</strong> — <code class="language-plaintext highlighter-rouge">np.linspace</code> creates 256 uniform bins across the action range</li> <li><strong>Clip and normalize</strong> — Ensure values fall within the tokenizer’s vocabulary range</li> <li><strong>Discretize</strong> — <code class="language-plaintext highlighter-rouge">np.digitize</code> converts continuous actions to bin indices</li> </ol> <p><strong>Why 256 bins?</strong> Following \(2^8\) (8-bit encoding), this gives a discretization granularity of roughly <strong>4mm per meter</strong> of workspace — the intrinsic error margin of the action representation.</p> <p>The reverse mapping (token → action) uses bin centers:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">decode_token_ids_to_actions</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">action_token_ids</span><span class="p">):</span>
    <span class="n">discretized_actions</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">tokenizer</span><span class="p">.</span><span class="n">vocab_size</span> <span class="o">-</span> <span class="n">action_token_ids</span>
    <span class="n">discretized_actions</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">clip</span><span class="p">(</span><span class="n">discretized_actions</span> <span class="o">-</span> <span class="mi">1</span><span class="p">,</span> 
                                   <span class="n">a_min</span><span class="o">=</span><span class="mi">0</span><span class="p">,</span> 
                                   <span class="n">a_max</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">bin_centers</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="n">bin_centers</span><span class="p">[</span><span class="n">discretized_actions</span><span class="p">]</span>
</code></pre></div></div> <h3 id="3-data-mixing-strategy">3. Data Mixing Strategy</h3> <p>A common misconception: OpenVLA was <strong>not</strong> trained by mixing internet-scale data with robot data. Instead, it fine-tunes a <strong>pretrained VLM</strong> (already trained on internet data) using only robot trajectory datasets.</p> <p>Two key filtering decisions:</p> <ol> <li><strong>Restrict to manipulation datasets</strong> with at least one 3rd-person camera and single-arm end-effector control — ensuring coherent I/O space.</li> <li><strong>Leverage Octo’s data mixture weights</strong> for balanced distribution across embodiments and tasks.</li> </ol> <p>The authors also removed the DROID dataset due to slow training and redistributed its weight across remaining datasets. The goal: enhance generalization and out-of-the-box deployment ability.</p> <h3 id="4-additional-thoughts">4. Additional Thoughts</h3> <blockquote> <p>“We found fine-tuning the vision encoder during VLA training to be crucial for good VLA performance.”</p> </blockquote> <p>This opens an interesting direction: <strong>Vision-Action (VA) models</strong> that use only visual features to generate actions, bypassing the language component entirely.</p> <p>Since all training data is robot-specific and the LLM output is action tokens (not natural language), it may be possible to run inference without industrial-grade GPUs. Perhaps this is another reason the team chose to open-source the entire codebase. As a student and engineer — big thanks to the open source community.</p>]]></content><author><name></name></author><category term="paper-review"/><category term="VLA"/><category term="robotics"/><category term="English"/><summary type="html"><![CDATA[A deep dive into OpenVLA's architecture — dual vision backbone, action tokenization, and data mixing strategy that enables a 7B model to achieve strong generalization in robotic manipulation.]]></summary></entry></feed>